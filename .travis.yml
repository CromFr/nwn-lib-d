language: d
d: dmd

before_install:
    - sudo apt-get -qq update
    - sudo apt-get install -y gcc-multilib

script:
    - dub build              --build=release
    - dub build              --build=release      :nwn-gff

    - dub test --arch=x86
    - dub test --arch=x86_64 --build=unittest-cov

    - dub test               --build=unittest-cov :nwn-gff

after_success:
    - bash <(curl -s https://codecov.io/bash)

notifications:
    email:
        - cromfr@gmail.com
branches:
    only:
        master
